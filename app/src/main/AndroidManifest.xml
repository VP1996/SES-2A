<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ses_2a_team_autoset">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_sb_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SES_2A_team_autoset">
        <activity android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeScreenStaff"
            android:label="@string/app_name"/>
        <activity android:name=".HomeScreenStudent"
            android:label="@string/app_name"/>
        <activity android:name=".Register"
            android:label="@string/app_name"/>
        <activity android:name=".SubjectClassesAdmin"
            android:label="@string/app_name"/>
        <activity android:name=".SubjectClassesStudent"
            android:label="@string/app_name"/>
        <activity android:name=".RequestPage"
            android:label="@string/app_name"/>
        <activity android:name=".QuizPageOne"
            android:label="@string/app_name"/>
        <activity android:name=".QuizPageTwo"
            android:label="@string/app_name"/>
        <activity android:name=".QuizPageThree"
            android:label="@string/app_name"/>
        <activity android:name=".QuizPageFour"
            android:label="@string/app_name"/>
        <activity android:name=".StudentProfile"
            android:label="@string/app_name"/>
        <activity android:name=".StudentQuizAnswers"
            android:label="@string/app_name"/>
        <activity android:name=".StudentEditProfile"
            android:label="@string/app_name"/>

    </application>

</manifest>
